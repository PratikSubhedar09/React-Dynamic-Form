const Form = require('./Form');
const Input = require('./Input');
const FormDataConsumer = require('./FormDataConsumer');

module.exports = {
  Form: Form,
  Input: Input,
  FormDataConsumer: FormDataConsumer,
};